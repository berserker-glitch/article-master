# Database
DB_URL="mysql://USER:PASSWORD@localhost:3306/article_master"

# App
APP_URL="http://localhost:3000"
AUTH_COOKIE_SECRET="change-me-long-random-string"

# Used to encrypt sensitive credentials in DB (AES-256-GCM)
# Generate 32 bytes, then base64 encode (example in README later)
APP_ENCRYPTION_KEY="base64-32-bytes-here"

# SMTP (email link signup)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user"
SMTP_PASS="smtp-pass"
SMTP_FROM="ArticleMaster <no-reply@example.com>"

# OpenRouter
OPENROUTER_API_KEY="or_..."
OPENROUTER_MODEL_CHAPTERS="google/gemini-2.0-flash-lite-001"
OPENROUTER_MODEL_WRITER="openai/gpt-5.2"
OPENROUTER_MODEL_CRITIC="moonshotai/kimi-k2-thinking"
